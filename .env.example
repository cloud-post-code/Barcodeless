DATABASE_URL=postgresql://postgres:postgres@localhost:5432/barcodeless
# Managed Postgres (Railway, Neon, etc.): require TLS — set if connections fail or you see 500s:
# DATABASE_SSL=true
CLIP_MODEL=ViT-L-14
CLIP_PRETRAINED=openai
DUPLICATE_THRESHOLD=0.92
TOP_K_RECALL=50
